import { Typography } from "./Typography/Typography";

export const TopBar = () => {
  return (
    <div className="p-4">
      <Typography variant="title28_semibold" tag="h2">
        SimpleChat
      </Typography>
    </div>
  );
};
